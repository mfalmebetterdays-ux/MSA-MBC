{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "NIXPACKS"
    },
    "deploy": {
      "startCommand": "sh -c 'python manage.py migrate && python manage.py collectstatic --noinput && echo \"Starting Gunicorn...\" && gunicorn mwasa.wsgi:application --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --access-logfile - --error-logfile - --log-level debug'",
      "restartPolicyType": "ON_FAILURE"
    }
  }